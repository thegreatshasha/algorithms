#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <vector>
#include <string>

using namespace std;

// Define it above so i don't have to write a prototype
void input(int &n, int &m, int &k) {
	//cout << "Enter n, m, k" << endl;
	cin >> n>> m >> k;

	//int colors = [][];
	//cout << "Enter n, m, k" << endl;
}

int main() {
	int n, m, k;

	// we are gonna print all suffixes corresponding to a string
  // might need to come up with the suffix tree
}
